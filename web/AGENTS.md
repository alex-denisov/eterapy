# web/

This is a stub. The full, vendor-neutral agent contract lives OUTSIDE this
repository: `AGENTS.md`, `docs/agents/`, the task board and tickets are kept in
the private repo **`alex-denisov/eterapy-docs`** (root `AGENTS.md` +
`docs/…`, same layout as the owner's local checkout — `docs/` is gitignored
here on purpose, B340).

Before doing anything in a session that does not already have `../AGENTS.md`
next to this repo (cloud sessions, fresh clones):

```bash
git clone https://github.com/alex-denisov/eterapy-docs.git ../eterapy-docs
```

then read `../eterapy-docs/AGENTS.md` and follow `../eterapy-docs/docs/agents/README.md`.
Tickets, BOARD and JOURNAL are edited in **that** repo and pushed to its `main`.
If the clone is refused, the Claude GitHub app is not installed on
`eterapy-docs` — say so to the owner instead of working without the contract.

No instructions live here — to avoid drift, this file points only at the entry,
never at specific topic files.
