/**
 * Окно диспута после сессии — 24 часа, в течение которых клиент может подать
 * жалобу и получить возврат после аппрува.
 *
 * Вынесено в отдельный чистый модуль (B583/B584-батч): величину читает и
 * `session-complete.ts` (серверный, тянет БД), и модель сплита Robokassa,
 * которая живёт в клиентских компонентах. Дублировать «24» в двух местах
 * нельзя: от неё зависит, до какого момента держится холд клиентского платежа,
 * то есть можно ли вообще сплитовать эту бронь.
 */
export const SESSION_DISPUTE_WINDOW_HOURS = 24;
