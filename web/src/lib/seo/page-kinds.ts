/**
 * B741 — словарь состояний и видов строки `seo_library_pages`.
 *
 * ⚠ ОТДЕЛЬНЫЙ МОДУЛЬ РАЗРЫВАЕТ КРУГ. `library-store` накладывает дописывание
 * (`mergeBackfill` из `backfill.ts`), а `backfill.ts` спрашивает статус строки
 * — и модули начали импортировать друг друга. Сегодня это работает: обе
 * ссылки разрешаются внутри функций, а не на загрузке. Но круговой импорт
 * ломается молча и не там, где его завели, поэтому общие константы вынесены
 * туда, откуда их берут оба, и ни один не зависит от другого.
 */

export const SEO_PAGE_STATUS = {
  draft: "DRAFT",
  published: "PUBLISHED",
  retired: "RETIRED",
} as const;

export const SEO_PAGE_KIND = {
  /** Новая страница, написанная по снятому спросу. */
  page: "PAGE",
  /** Дописанное тело для карточки редакционного корпуса. */
  backfill: "BACKFILL",
} as const;
