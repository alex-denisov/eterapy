import { librarySection, type AnonymousLibraryEntry } from "@/data/anonymous-library";
import { isEchoOf } from "@/lib/library-depth";
import type { LibraryTopic } from "@/lib/library-cta";

export const LIBRARY_EDITORIAL_DATE = "2026-07-22";
export const LIBRARY_EDITORIAL_DATE_RU = "22 июля 2026";

const RU_MONTHS = [
  "января", "февраля", "марта", "апреля", "мая", "июня",
  "июля", "августа", "сентября", "октября", "ноября", "декабря",
] as const;

/**
 * B710 — дата редактуры принадлежит ЗАПИСИ, а не корпусу целиком.
 *
 * Общая константа была верна, пока весь корпус вышел одним днём. Запись,
 * опубликованная позже, показывала бы дату проверки РАНЬШЕ собственного
 * появления — и в тексте страницы, и в `datePublished`/`dateModified`. Это уже
 * не «неточность оформления», а недостоверная запись о редакционной проверке.
 */
export function libraryEditorialDate(entry: AnonymousLibraryEntry): string {
  return entry.reviewedAt ?? LIBRARY_EDITORIAL_DATE;
}

export function libraryEditorialDateRu(entry: AnonymousLibraryEntry): string {
  const iso = libraryEditorialDate(entry);
  if (iso === LIBRARY_EDITORIAL_DATE) return LIBRARY_EDITORIAL_DATE_RU;
  const [year, month, day] = iso.split("-").map(Number);
  const monthName = RU_MONTHS[month - 1];
  if (!year || !monthName || !day) return LIBRARY_EDITORIAL_DATE_RU;
  return `${day} ${monthName} ${year}`;
}

type TopicGuidance = {
  checks: readonly string[];
  humanSupport: string;
};

const TOPIC_GUIDANCE: Record<LibraryTopic, TopicGuidance> = {
  "Отношения": {
    checks: [
      "Что произошло наблюдаемо, без попытки угадать мысли другого человека.",
      "О чём уже удалось поговорить прямо, а что пока существует только как предположение.",
      "Какая граница или потребность сейчас важнее немедленного ответа.",
    ],
    humanSupport: "Если в отношениях есть давление, угрозы, контроль или страх за безопасность, лучше не оставаться с этим в одиночку и обратиться за очной поддержкой.",
  },
  "Повторяется одно и то же": {
    checks: [
      "В какой момент похожая ситуация начинает повторяться.",
      "Какое действие или бездействие обычно следует прямо перед знакомым результатом.",
      "Какой небольшой выбор можно изменить без резкого переворота всей жизни.",
    ],
    humanSupport: "Если повторяющийся сценарий заметно ограничивает жизнь или причиняет длительное страдание, разговор с квалифицированным специалистом может дать больше опоры, чем самостоятельный разбор.",
  },
  "Тревога и состояние": {
    checks: [
      "Какие факты происходят сейчас, а какие сценарии дорисовывает тревога.",
      "Как долго длится состояние и влияет ли оно на сон, питание, работу и обычные дела.",
      "Что хотя бы немного снижает напряжение без вреда для вас.",
    ],
    humanSupport: "Если состояние держится дольше двух недель, усиливается или мешает обычной жизни, стоит обратиться к профильному специалисту. При риске для безопасности нужна срочная помощь.",
  },
  "Работа и деньги": {
    checks: [
      "Какие последствия решения можно посчитать или проверить заранее.",
      "Что является реальным ограничением, а что привычным страхом перемен.",
      "Как выглядит обратимый эксперимент вместо решения навсегда.",
    ],
    humanSupport: "Для решений с существенными финансовыми или юридическими последствиями используйте профильную консультацию и проверяемые расчёты, а не только рефлексивный материал.",
  },
  "Одиночество": {
    checks: [
      "Какого именно контакта не хватает: присутствия, понимания, совместных дел или новых знакомств.",
      "С кем уже есть безопасная возможность сделать небольшой шаг навстречу.",
      "Что мешает попросить о контакте прямо и без самообвинения.",
    ],
    humanSupport: "Если одиночество сопровождается потерей сил, надежды или интереса к жизни, важно обратиться к человеку, которому вы доверяете, или к профильному специалисту.",
  },
  "Выбор и решения": {
    checks: [
      "Что вы приобретаете и что теряете в каждом варианте.",
      "Какая часть решения обратима, а какая потребует отдельной подготовки.",
      "Какой факт сильнее всего изменил бы ваш выбор, если бы вы узнали его сегодня.",
    ],
    humanSupport: "Когда решение затрагивает здоровье, право, крупные деньги или безопасность, рефлексию нужно дополнить консультацией профильного специалиста.",
  },
  "Про себя": {
    checks: [
      "Что вы чувствуете и чего хотите, если временно убрать ожидания других людей.",
      "Какое убеждение о себе здесь звучит как факт, хотя его можно проверить.",
      "Какой небольшой поступок поможет получить новую информацию о себе.",
    ],
    humanSupport: "Если вопрос связан с длительным ухудшением состояния или заметно мешает повседневной жизни, лучше дополнить самостоятельное исследование поддержкой квалифицированного специалиста.",
  },
  "Сны и символы": {
    checks: [
      "Какая эмоция во сне была самой сильной и что напомнило её после пробуждения.",
      "Какие личные ассоциации вызывает образ, помимо готового значения из сонника.",
      "Что происходило в жизни накануне сна и повторяется ли сюжет в похожие периоды.",
    ],
    humanSupport: "Толкование сна не является прогнозом или диагнозом. Если кошмары часто повторяются, нарушают сон или связаны с тяжёлым переживанием, полезно обсудить это с профильным специалистом.",
  },
  "Таро": {
    checks: [
      "Как был сформулирован вопрос и не просит ли он карты принять решение вместо вас.",
      "Какая часть значения действительно совпадает с наблюдаемыми фактами.",
      "Какой вариант действия остаётся безопасным и обратимым независимо от расклада.",
    ],
    humanSupport: "Таро используется как символический язык для размышления, а не как доказательство будущего. Решения о здоровье, безопасности, праве и крупных деньгах нельзя принимать по картам.",
  },
  "Матрица судьбы": {
    checks: [
      "Какая методика и формула использованы в расчёте.",
      "Какие реальные примеры подтверждают трактовку, а какие ей противоречат.",
      "Не превращается ли название аркана или программы в неизменный ярлык о себе.",
    ],
    humanSupport: "Матрица судьбы является символической системой, а не научным тестом личности и не прогнозом событий. Проверяемые решения требуют фактов и профильных консультаций.",
  },
  "Натальная карта": {
    checks: [
      "Насколько точны дата, время и место рождения и какие элементы зависят от этих данных.",
      "Где интерпретация описывает широкую тему, а где вы видите конкретный жизненный пример.",
      "Оставляет ли трактовка место опыту, среде и вашему выбору.",
    ],
    humanSupport: "Натальная карта является символической моделью, а не диагнозом, доказательством характера или гарантией событий. Для важных решений опирайтесь на факты и компетентную помощь.",
  },
  "Совместимость": {
    checks: [
      "Что в отношениях подтверждается поступками и прямыми договорённостями.",
      "Какие различия пара уже умеет обсуждать и проживать вместе.",
      "Не подменяет ли общий балл живой разговор и согласие другого человека.",
    ],
    humanSupport: "Расчёт совместимости не измеряет качество отношений и не выносит паре вердикт. При контроле, насилии или угрозах безопасности нужен не расчёт, а живая поддержка.",
  },
  "Имя и фамилия": {
    checks: [
      "Какие личные и семейные истории связаны с именем или фамилией.",
      "Что в решении важно вам, а что продиктовано страхом чужой трактовки.",
      "Какие последствия реальны и проверяемы, а какие существуют только как символическая версия.",
    ],
    humanSupport: "Значение имени и числовой код фамилии не определяют характер или судьбу. Это культурные и символические способы размышления, а не основание ограничивать выбор.",
  },
};

export type LibraryFaq = {
  question: string;
  answer: string;
};

export function libraryChecks(entry: AnonymousLibraryEntry): readonly string[] {
  const own = entry.perspectives.length > 0
    ? [...new Set(entry.perspectives)]
    : [...TOPIC_GUIDANCE[entry.topic].checks];
  /**
   * B714 — «первый шаг» не повторяется в списке проверок.
   *
   * У части записей `freeFragment` дословно совпадает с первым пунктом
   * `perspectives`, и страница печатала одну фразу дважды: в списке «что можно
   * проверить» и крупным шрифтом в блоке «первый шаг». Живой пример —
   * `/library/net-nastoyashchikh-druzey`.
   *
   * Убираем из списка, а не из блока: «первый шаг» — единственное место, где
   * фраза стоит как призыв к действию, и именно там она полезна.
   */
  /**
   * Пробой служит РЕЗУЛЬТАТ `libraryFirstStep`, а не поле `freeFragment`: без
   * своего фрагмента первым шагом становится `perspectives[0]`, и тогда
   * дублируется как раз он. Проверка по полю пропускала ровно этот случай —
   * самый частый в корпусе.
   */
  const firstStep = libraryFirstStep(entry).trim();
  if (!firstStep) return own;
  const filtered = own.filter((item) => !isEchoOf(item, [firstStep]));
  // Если после чистки не осталось ничего, честнее показать исходный список,
  // чем пустой раздел: пустой заголовок читается как поломка страницы.
  return filtered.length > 0 ? filtered : own;
}

export function libraryFirstStep(entry: AnonymousLibraryEntry): string {
  return entry.freeFragment ?? entry.perspectives[0] ?? "Запишите отдельно факты, чувства и предположения. Начните с одного действия, которое можно отменить или уточнить.";
}

export function libraryHumanSupport(entry: AnonymousLibraryEntry): string {
  return TOPIC_GUIDANCE[entry.topic].humanSupport;
}

/**
 * B714 — САМОПОВТОР СНЯТ, И СНЯТ ОН ЗДЕСЬ, А НЕ В ДАННЫХ.
 *
 * Живая проверка `/library/net-nastoyashchikh-druzey` до правки: фраза
 * «Близость строится через повторный контакт…» стояла на странице ТРИЖДЫ — в
 * списке «что можно проверить», в блоке «первый шаг» и в ответе FAQ. Причина
 * была в шаблоне: ответы FAQ собирались из `summary` и `firstStep`, то есть
 * дословно из того, что страница уже напечатала выше. Для поисковика это
 * 60 уникальных слов, размноженные до 200.
 *
 * Правило простое: ответ FAQ не имеет права повторять уже напечатанное. Если
 * своего ответа нет, честнее показать меньше вопросов, чем те же слова под
 * новым заголовком.
 */
function withoutEchoes(faqs: LibraryFaq[], alreadyPrinted: readonly string[]): LibraryFaq[] {
  return faqs.filter((faq) => !isEchoOf(faq.answer, alreadyPrinted));
}

export function libraryFaqs(entry: AnonymousLibraryEntry): LibraryFaq[] {
  if (entry.faqs?.length) return entry.faqs;
  const printed = [entry.summary, libraryFirstStep(entry), ...libraryChecks(entry)];
  if (librarySection(entry) === "symbolic") {
    return withoutEchoes([
      {
        question: `Можно ли считать трактовку по теме «${entry.topic}» точным прогнозом?`,
        answer: "Нет. Символический разбор помогает поставить вопросы и увидеть возможные связи, но не доказывает характер, судьбу или будущие события.",
      },
      {
        question: "Что сильнее всего меняет значение в этой ситуации?",
        answer: "Значение символа меняют формулировка вопроса, ваши личные ассоциации и наблюдаемые факты, а не только готовая трактовка.",
      },
      {
        question: "Как использовать этот разбор без фатализма?",
        answer: "Относитесь к результату как к гипотезе для проверки и не передавайте ему право решать за вас.",
      },
    ], printed);
  }
  return withoutEchoes([
    {
      question: "Можно ли по этой странице точно понять, почему это произошло?",
      answer: "Нет. Разбор даёт рабочую гипотезу для размышления, а не установленный факт о вас или другом человеке.",
    },
    {
      question: "Заменяет ли этот материал консультацию психолога?",
      answer: "Нет. Материал помогает сформулировать вопрос и увидеть возможные направления для проверки. Он не является диагностикой, лечением или психологической консультацией.",
    },
  ], printed);
}

function compactQuestion(question: string, maxLength = 54): string {
  const normalized = question.replace(/[«»]/g, "").trim();
  if (normalized.length <= maxLength) return normalized;
  const shortened = normalized.slice(0, maxLength - 1).replace(/\s+\S*$/, "").trim();
  return `${shortened}…`;
}

function compactText(text: string, maxLength: number): string {
  const normalized = text.replace(/\s+/g, " ").trim();
  if (normalized.length <= maxLength) return normalized;
  const shortened = normalized.slice(0, maxLength - 1).replace(/\s+\S*$/, "").replace(/[,:;.!?]+$/, "").trim();
  return `${shortened}…`;
}

export function libraryMetaTitle(entry: AnonymousLibraryEntry): string {
  return entry.seo?.metaTitle ?? `${compactQuestion(entry.question)} | ETerapy`;
}

export function libraryMetaDescription(entry: AnonymousLibraryEntry): string {
  return compactText(
    entry.seo?.metaDescription ?? `${entry.summary} Короткий разбор и первый безопасный шаг.`,
    158,
  );
}
