// INC-078: у корневого `loading.tsx` была вторая, неявная работа — он служил
// Suspense-границей, без которой `useSearchParams()` не проходит пререндер
// (CSR bailout). При переезде границы на сегменты это всплыло сборкой: формы
// входа и восстановления пароля читают query-параметры. Граница здесь нужна
// по существу, а 404 этим маршрутам не требуется.
export { RouteLoading as default } from "@/components/route-loading";
