import { permanentRedirect } from "next/navigation";

/**
 * B593 (владелец 2026-07-27): «Ежедневная практика» как отдельный экран
 * закрыта — весь её ритуал (вопрос дня, полоса недели, серия) с B512 живёт на
 * «Дневнике» и рендерится тем же компонентом `DailyPracticeActions`. Экран
 * остался дублем, на который вели рекомендации с Главной.
 *
 * Страница не удалена, а превращена в 308: на неё ведут уже отправленные
 * письма, push-уведомления, ссылки Telegram-бота и закладки. Убрать URL —
 * значит превратить каждую такую ссылку в 404.
 */
export default function RetiredCabinetPracticeRedirect() {
  permanentRedirect("/cabinet/diary");
}
