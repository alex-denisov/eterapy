-- B379 (M26): «контекст встречи» — что клиент хочет разобрать. Задаётся клиентом
-- при первой записи к специалисту, виден только клиенту/специалисту/админам.
-- IF NOT EXISTS — идемпотентность на случай частичного применения.

ALTER TABLE "bookings" ADD COLUMN IF NOT EXISTS "meeting_context" TEXT;
