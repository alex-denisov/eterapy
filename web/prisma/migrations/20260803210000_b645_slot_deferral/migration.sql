-- B645: счётчик переносов материала в следующий слот контент-плана.
--
-- Отдельная колонка, а не переиспользование `recovery_count`: тот считает
-- перевыпуски текста, этот — переносы времени. Смешав их, потеряли бы обе
-- величины и вместе с ними обе границы (перевыпусков 2, переносов 3).
ALTER TABLE "external_publications"
  ADD COLUMN IF NOT EXISTS "deferral_count" INTEGER NOT NULL DEFAULT 0;
