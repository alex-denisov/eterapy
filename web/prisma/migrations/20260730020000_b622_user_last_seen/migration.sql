-- B622: «Последний вход» в суперадминке брался только из LOGIN-аудита, который
-- пишется в момент самой аутентификации. При живой JWT-сессии человек ходит по
-- сайту неделями, не создавая событий, и столбец замирал на дате регистрации.
-- Отдельная отметка присутствия обновляется не чаще раза в 15 минут.
ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "last_seen_at" TIMESTAMP(3);
